# 《一夫当关》开发日志

## 日志格式说明

每条日志包含以下字段：
- **日期**：修改日期
- **类型**：feat（新功能）/ fix（修复）/ refactor（重构）/ chore（杂项）/ docs（文档）
- **范围**：修改涉及的模块或文件
- **描述**：修改内容概述
- **详细说明**：修改原因、实现方式、影响范围

---

## 2026-05-02

### 1. 项目初始化与 Git 仓库建立

| 字段 | 内容 |
|------|------|
| **类型** | chore |
| **范围** | 项目根目录 |
| **描述** | 初始化 Git 仓库，关联远程仓库 `https://github.com/XwXchaser/threeKingdomSlayer.git` |
| **详细说明** | 在 `C:/threeKingdomSlayer` 执行 `git init`，创建 `.gitignore`（排除 Library/、Temp/、Logs/、Build/、.idea/ 等），添加所有文件并提交初始版本 `83e4c15`，推送至 GitHub `main` 分支 |

### 2. 阶段三核心战斗系统脚本全部完成

| 字段 | 内容 |
|------|------|
| **类型** | feat |
| **范围** | Core / Enemy / Managers / Player / Wave / UI |
| **描述** | 完成 MVP 阶段三全部 17 个脚本的编写 |
| **详细说明** | 详见下方各模块清单 |

#### 2.1 Core 层（6 个文件）

| 文件 | 说明 |
|------|------|
| `Column.cs` | 列数据结构，维护 `List<Enemy>`，提供 `GetFrontEnemy()` 查询最前排 |
| `ColumnManager.cs` | 5 列管理器，添加/移除/更新/补齐逻辑 |
| `EnemyConfig.cs` | 敌人 ScriptableObject 配置（生命/架势/速度/6 种弱点倍率） |
| `HeroConfig.cs` | 武将 ScriptableObject 配置（生命/攻击/冷却/技能参数） |
| `StageConfig.cs` | 关卡配置（波次列表/连杀奖励/通关奖励/透明度/阵型参数） |
| `RowFormation.cs` | 阵型计算器 + `RowFormationPreset` ScriptableObject（方案 A 预设表 + 方案 B 公式） |

#### 2.2 Enemy 层（2 个文件）

| 文件 | 说明 |
|------|------|
| `Enemy.cs` | 敌人实体，状态机（Idle/Moving/Attacking/Stunned/Launched/Dead），伤害系统（含 6 种弱点倍率），阵型位置更新，透明度渐变（MaterialPropertyBlock） |
| `EnemyPool.cs` | 按 enemyId 分池的对象池，支持预创建和动态扩容 |

#### 2.3 Managers 层（2 个文件）

| 文件 | 说明 |
|------|------|
| `EnemyManager.cs` | 单例，注册/死亡/前进/攻击事件，列管理协调 |
| `StageController.cs` | 关卡状态机（None→Starting→InProgress→Victory/Defeat），`GetFormationOffset()` 阵型查询 |

#### 2.4 Player 层（3 个文件）

| 文件 | 说明 |
|------|------|
| `PlayerState.cs` | 单例，生命/复活/6 种攻击冷却/击杀/铜钱/关卡状态 |
| `AttackSystem.cs` | 6 种攻击实现（Stab/Slash/Pierce/Sweep/Launch/Parry） |
| `InputManager.cs` | 鼠标+触摸手势识别（点击/长按/水平滑动/垂直滑动/横向跨区） |

#### 2.5 Wave 层（1 个文件）

| 文件 | 说明 |
|------|------|
| `WaveSpawner.cs` | 协程驱动波次生成，`WaitForWaveClear()` 等待每波清空 |

#### 2.6 UI 层（2 个文件）

| 文件 | 说明 |
|------|------|
| `BattleHUD.cs` | 战斗 HUD（生命/复活/击杀/铜钱/波次/冷却指示器） |
| `MainMenuUI.cs` | 主菜单（标题/开始游戏/退出） |

### 3. BUG 修复（3 个）

#### 3.1 EnemyPool 回收 BUG

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `EnemyPool.cs` |
| **描述** | `enemyId` 在 `ResetEnemy()` 之后读取导致始终为 -1 |
| **详细说明** | `ReturnEnemy()` 中先调用了 `enemy.ResetEnemy()`（将 `config` 置 null），然后才读取 `enemyId`，导致所有敌人被错误地回收进 `pools[-1]` 池。修复：将 `enemyId` 读取移到 `ResetEnemy()` 之前 |

#### 3.2 敌人攻击不扣血

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `EnemyManager.cs` |
| **描述** | `OnEnemyAttackPlayer()` 中 `PlayerState` 引用被注释掉 |
| **详细说明** | 敌人攻击玩家时未实际调用 `PlayerState.TakeDamage()`，导致玩家不掉血。修复：实际调用 `PlayerState.Instance?.TakeDamage(damage)` |

#### 3.3 StageController null 引用

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `Enemy.cs` |
| **描述** | `GetAlphaForRow()` 中 `StageController.Instance` 为 null 时静默使用默认值 |
| **详细说明** | 在编辑器测试或场景未正确挂载时，`StageController.Instance` 可能为 null，原代码静默使用默认透明度数组，不利于排查问题。修复：添加 `Debug.LogWarning` 警告日志 |

### 4. 阵型系统（梯形/扇形内收）

| 字段 | 内容 |
|------|------|
| **类型** | feat |
| **范围** | `RowFormation.cs` / `StageConfig.cs` / `StageController.cs` / `Enemy.cs` |
| **描述** | 实现敌人排列"内收"视觉效果，支持预设表和公式两种方案 |
| **详细说明** | **方案 A**：`RowFormationPreset` ScriptableObject，策划可微调每排精确 X 偏移值。**方案 B**：公式 `Lerp(maxSpread, minSpread, Pow(row/maxRow, powerCurve))` 动态计算。配置参数已同步到 `StageConfig.cs` → `StageController.cs` → `Enemy.cs.UpdateWorldPosition()` |

### 5. .meta 文件跟踪

| 字段 | 内容 |
|------|------|
| **类型** | chore |
| **范围** | 13 个 `.meta` 文件 |
| **描述** | 发现 13 个脚本的 `.meta` 文件未被 Git 跟踪，已添加并推送 |
| **详细说明** | Unity 的 `.meta` 文件包含 GUID 和导入设置，是 Unity 项目必需的元数据。缺少 `.meta` 文件会导致其他机器克隆后 Unity 无法识别文件引用关系。提交 `66937a6` 添加了全部 13 个 `.meta` 文件 |

### 6. 编译错误修复：Enemy.Die() 访问权限

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `Enemy.cs` |
| **描述** | `Enemy.Die()` 调用 `EnemyManager.OnEnemyDied()` 但该方法为 `private` |
| **详细说明** | **错误信息**：`error CS0122: 'EnemyManager.OnEnemyDied(Enemy)' is inaccessible due to its protection level`。**根因**：`EnemyManager.OnEnemyDied()` 是 `private` 方法，但 `Enemy.Die()` 中直接调用了 `EnemyManager.Instance?.OnEnemyDied(this)`。**修复**：移除 `Enemy.Die()` 中对 `EnemyManager.OnEnemyDied()` 的直接调用。死亡流程改为完全通过事件驱动：`Enemy.Die()` → 触发 `OnDeath` 事件 → `EnemyManager` 通过 `RegisterEnemy()` 时订阅的 `enemy.OnDeath += OnEnemyDied` 处理回收、列移除、事件通知。提交 `c81687f` |

---

## 项目状态总览

### 已完成（阶段一 ~ 阶段三）

- [x] 阶段一：项目初始化与环境搭建
- [x] 阶段二：数据层 ScriptableObject 配置系统（EnemyConfig / HeroConfig / StageConfig / WaveConfig / RowConfig）
- [x] 阶段三：核心战斗系统脚本（Column / ColumnManager / Enemy / EnemyPool / EnemyManager / StageController / RowFormation）
- [x] 阶段四：玩家攻击系统（PlayerState / AttackSystem / InputManager）
- [x] 阶段五：波次生成系统（WaveSpawner / StageController 关卡流程）
- [x] 阶段六：UI 系统（BattleHUD / MainMenuUI）
- [x] Git 仓库建立并推送至 GitHub

### 待办（需手动操作）

- [ ] 创建 `MainMenu` 和 `Battle` 场景，在 `EditorBuildSettings` 中注册
- [ ] 创建 ScriptableObject 实例：骷髅兵(EnemyConfig)、赵云(HeroConfig)、第1关(StageConfig，含3~5波敌人配置)
- [ ] 创建敌人预制体（带 Enemy 组件和 Renderer），注册到 EnemyPool
- [ ] 在 Battle 场景中挂载所有管理器组件
- [ ] 在 MainMenu 场景中挂载 MainMenuUI
- [ ] 设置材质为透明渲染模式（Fade/Transparent）
- [ ] 阶段七：美术资源占位
- [ ] 阶段八：整合与测试

---

## 2026-05-03

### 1. UI 文字模糊修复 — TextMeshPro 迁移

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `BattleHUD.cs` / `Battle.scene` / `MainMenu.scene` |
| **描述** | 将所有 Legacy Text 组件替换为 TextMeshProUGUI，修复文字模糊问题 |
| **详细说明** | Unity 旧版 Text 组件在高分辨率下渲染模糊。将 4 处 BattleHUD Text 和 3 处 MainMenu Text 全部替换为 TextMeshPro。`BattleHUD.cs` 中所有 `Text` 引用改为 `TMP_Text`，添加 `using TMPro`。修复 `LiberationSans SDF.asset` 的 Shader 从 Mobile 改为 Standard。 |

### 2. 攻击波系统重构 — DOTween 弹射动效

| 字段 | 内容 |
|------|------|
| **类型** | feat |
| **范围** | `AttackWave.cs` (新建) / `AttackSystem.cs` |
| **描述** | 将横扫/穿刺/戳击从"原地生成"改为"弹射飞行"动效，使用 DOTween Sequence 驱动 |
| **详细说明** | **Travel 模式**（Sweep/Pierce/Stab）：DOTween Sequence 驱动 `DOMoveZ` 沿 Z 轴飞行。**Fixed 模式**（Slash/Launch）：保持 Update() 时间驱动。戳击特有收回动画：刺出后 `DOMoveZ` 反向收回 + 并行 `DOFade` 淡出。位置策略：X/Y 由 `GetWavePosition` 计算（列对齐 + 高度），Z 沿用 Prefab 自身 Z 坐标作为飞行起点，Rotation 保持 Prefab 原始角度。 |

### 3. 攻击飞行方向修复

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `AttackWave.cs` |
| **描述** | 修复 Wave 飞行终点计算错误：原取最近敌人 Z - 偏移，现取最远敌人 Z + 偏移 |
| **详细说明** | 摄像机 Z=-10，敌人 Z=7-9，Prefab Z=-1.19。原逻辑 `endTravelZ = backmostZ - 3` 导致 Wave 飞到 Z=4 即停，从未到达敌人区域。同时 `CheckHitThresholds` 比较方向逆转。修复：自动判断旅行方向，终点取最远敌人 Z + 偏移，阈值按相遇顺序排序并自适应比较逻辑。 |

### 4. 戳击动画优化

| 字段 | 内容 |
|------|------|
| **类型** | feat |
| **范围** | `AttackWave.cs` |
| **描述** | 戳击固定时长 1.2 秒（刺出 0.45s + 收回 0.75s），终点改为最近敌人前方 2.5 单位 |
| **详细说明** | 戳击不再穿到敌人身后，刺出至最近敌人前 Z-2.5 即返回。命中改用时间驱动（刺出前立即命中所有目标），视觉上枪刺到敌人前方即收回。Sweep/Pierce 保持速度制（ProjectileSpeed=8）穿过全列后淡出。 |

### 5. 戳击列判定修复

| 字段 | 内容 |
|------|------|
| **类型** | fix |
| **范围** | `InputManager.cs` |
| **描述** | 修复两处列判定问题：(1) 射线 Z=0 平面投影畸变导致点击 A 列误伤 B 列；(2) 空列自动跳转邻列 |
| **详细说明** | (1) 改为 `Camera.WorldToScreenPoint` 将敌人投影到屏幕空间，直接比较屏幕 X 距离，消除 Z 深度投影畸变。(2) 增加半列宽距离阈值（`Screen.width/10`），超过阈值返回 -1 阻断攻击，防止空列补齐期间自动跳转到邻列。 |

### 6. Android 签名配置

| 字段 | 内容 |
|------|------|
| **类型** | chore |
| **范围** | `ProjectSettings/ProjectSettings.asset` |
| **描述** | 配置 Android Keystore 签名密码 |
| **详细说明** | `PlayerSettings.Android.keystorePass` 和 `keyaliasPass` 已设置，解决 "Can not sign the application" 构建失败问题。 |

### 7. 美术资源导入

| 字段 | 内容 |
|------|------|
| **类型** | feat |
| **范围** | `Assets/Sprites/` / `Assets/Prefabs/` |
| **描述** | 导入战斗背景、敌人精灵、攻击特效精灵，创建 Slash/Stab Prefab |
| **详细说明** | 导入 `backgroun_battle.png`、`background_MENU.jpg`、`Enemy_1.png`、`Enemy_2.png`、`slash.png`、`stab.png` 等精灵。创建 `Slash.prefab`（Z=-1.19, rotation=90°X）、`Stab.prefab`（Z=-5.71, rotation=90°X）。新增 `Enemy_102.prefab` 和 `Enemy_Shilde.asset` 配置。 |
